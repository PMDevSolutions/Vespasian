# Fidelity Report

**1** note(s) — 0 error, 0 warning, 1 info. **2** step(s) awaiting the wix-site-builder agent.

## Translation & execution losses

| Severity | Code | Detail |
|---|---|---|
| info | `spacing-css-only` | Spacing scale lives in global.css custom properties (Wix has no spacing tokens). |

## Pending agent steps

- **editor-1** (`pages.create`, editor): No editor driver attached — hand "pages.create" to the wix-site-builder agent (input in the BuildPlan step).
- **editor-2** (`canvas.composeSection`, editor): No editor driver attached — hand "canvas.composeSection" to the wix-site-builder agent (input in the BuildPlan step).
